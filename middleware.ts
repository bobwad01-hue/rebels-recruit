import {createServerClient} from '@supabase/ssr';import {NextResponse,type NextRequest} from 'next/server';
export async function middleware(request:NextRequest){let response=NextResponse.next({request});const supabase=createServerClient(process.env.NEXT_PUBLIC_SUPABASE_URL!,process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!,{cookies:{getAll:()=>request.cookies.getAll(),setAll:(cs)=>{cs.forEach(({name,value})=>request.cookies.set(name,value));response=NextResponse.next({request});cs.forEach(({name,value,options})=>response.cookies.set(name,value,options))}}});const {data:{user}}=await supabase.auth.getUser();const pathname=request.nextUrl.pathname;if(!user&&!pathname.startsWith('/login')&&!pathname.startsWith('/signup')&&pathname!=='/'&&!pathname.startsWith('/auth'))return NextResponse.redirect(new URL('/login',request.url));if(user&&(pathname==='/login'||pathname==='/signup')){const {data:p}=await supabase.from('profiles').select('app_role').eq('id',user.id).single();return NextResponse.redirect(new URL(p?.app_role==='athlete'?'/dashboard':'/advisors',request.url))}if(user){const {data:p}=await supabase.from('profiles').select('app_role').eq('id',user.id).single();const role=p?.app_role||'athlete';const staffOnly=['/advisors','/organization','/exports'];const playerOnly=['/dashboard','/profile','/colleges','/activity','/reminders','/social'];if(role==='athlete'&&staffOnly.some(x=>pathname===x||pathname.startsWith(x+'/')))return NextResponse.redirect(new URL('/dashboard',request.url));if(role!=='athlete'&&playerOnly.some(x=>pathname===x||pathname.startsWith(x+'/')))return NextResponse.redirect(new URL('/advisors',request.url))}return response}
export const config={matcher:['/((?!_next/static|_next/image|favicon.ico).*)']};
