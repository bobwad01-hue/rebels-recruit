import Link from 'next/link';
import {ArrowRight,CheckCircle2,Clock3} from 'lucide-react';
import {eventWorkflow,type WorkflowRole} from '@/lib/event-recruiting-workflow';

export default function EventWorkflowPanel({event,role='athlete',emailReminder,debrief,athleteId,title,transformHref}:{event:any;role?:WorkflowRole;emailReminder?:any;debrief?:any;athleteId?:string;title?:string;transformHref?:(href:string)=>string}){
 const flow=eventWorkflow({event,role,emailReminder,debrief,athleteId}),hrefFor=(href:string)=>transformHref?transformHref(href):href;
 return <section className="card p-4 sm:p-5"><div className="rr-eyebrow">EVENT WORKFLOW</div><div className="flex flex-col sm:flex-row sm:items-end sm:justify-between gap-2"><div><h2 className="font-black text-xl">{title||'Turn the event into recruiting progress'}</h2><p className="muted text-sm mt-1">{flow.days===null?'Work the event before, during and after.':flow.past?`Event was ${Math.abs(flow.days)} day${Math.abs(flow.days)===1?'':'s'} ago.`:flow.today?'Event is today.':`${flow.days} day${flow.days===1?'':'s'} until the event.`}</p></div><div className="status-pill">{flow.current?.status==='done'?'On track':'Current focus'}</div></div><div className="grid md:grid-cols-2 gap-3 mt-4">{flow.steps.map((s,i)=>{const body=<><div className="flex items-start gap-3"><div className={`h-8 w-8 rounded-full border flex items-center justify-center shrink-0 ${s.status==='done'?'bg-slate-900 text-white':s.status==='now'?'border-red-300 bg-red-50 text-red-700':'bg-white'}`}>{s.status==='done'?<CheckCircle2 size={16}/>:<span className="font-black text-xs">{i+1}</span>}</div><div className="min-w-0"><div className="flex items-center gap-2 flex-wrap"><div className="font-black text-sm">{s.title}</div>{s.status==='now'&&<span className="pill bg-red-50 text-red-700">Now</span>}{s.status==='done'&&<span className="pill">Done</span>}</div><p className="muted text-xs mt-1">{s.detail}</p>{s.href&&<div className="font-black text-xs mt-2 inline-flex items-center gap-1">Open <ArrowRight size={12}/></div>}</div></div></>;return s.href?<Link key={s.id} href={hrefFor(s.href)} className={`rounded-xl border p-3 rr-interactive-card ${s.status==='now'?'border-red-200 bg-red-50/50':''}`}>{body}</Link>:<div key={s.id} className="rounded-xl border p-3">{body}</div>})}</div><div className="mt-4 text-xs muted flex items-center gap-1.5"><Clock3 size={13}/>Going should create preparation work; debrief and follow-up should close the loop afterward.</div></section>
}
