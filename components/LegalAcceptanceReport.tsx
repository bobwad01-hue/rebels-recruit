'use client';
export default function LegalAcceptanceReport({rows}:{rows:any[]}){function download(){const headers=['User','Email','Terms Version','Privacy Version','Accepted At','Method','Context'];const esc=(v:any)=>`"${String(v??'').replaceAll('"','""')}"`;const csv=[headers.map(esc).join(','),...rows.map(r=>[r.user,r.email,r.terms_version,r.privacy_version,r.accepted_at,r.method,r.context].map(esc).join(','))].join('\n');const blob=new Blob([csv],{type:'text/csv;charset=utf-8'});const url=URL.createObjectURL(blob);const a=document.createElement('a');a.href=url;a.download=`legal-acceptance-${new Date().toISOString().slice(0,10)}.csv`;a.click();URL.revokeObjectURL(url)}return <button className="btn" onClick={download} disabled={!rows.length}>Download Acceptance CSV</button>}
