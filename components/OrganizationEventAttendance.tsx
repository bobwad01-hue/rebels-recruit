'use client';
import {useEffect,useState} from 'react';import {Check,X,Users} from 'lucide-react';import {createClient} from '@/lib/supabase-browser';
export default function OrganizationEventAttendance({event}:{event?:any}){const c=createClient();const [role,setRole]=useState(''),[status,setStatus]=useState(''),[attendance,setAttendance]=useState<any>(null),[busy,setBusy]=useState(false);
useEffect(()=>{if(!event?.id)return;(async()=>{const {data:{user}}=await c.auth.getUser();if(!user)return;const {data:p}=await c.from('profiles').select('app_role').eq('id',user.id).single();const r=p?.app_role||'athlete';setRole(r);if(r==='athlete'){const res=await fetch('/api/events/rsvp',{cache:'no-store'});if(res.ok){const d=await res.json();setStatus(d.statuses?.[event.id]||'')}}else{const res=await fetch('/api/events/rsvp?scope=organization',{cache:'no-store'});if(res.ok){const d=await res.json();setAttendance(d.events?.[event.id]||{going:[],notGoing:[]})}}})()},[event?.id]);
async function choose(next:'going'|'not_going'){if(!event?.id)return;setBusy(true);const res=await fetch('/api/events/rsvp',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({eventId:event.id,status:next,eventName:event.name,eventDate:event.date})});if(res.ok)setStatus(next);setBusy(false)}
if(!event?.id||!role)return null;if(role==='athlete')return <div className="mt-4 pt-4 border-t"><div className="text-xs font-bold mb-2">Are you attending?</div><div className="flex gap-2"><button disabled={busy} onClick={()=>choose('going')} className={`btn py-1.5 px-2.5 text-xs ${status==='going'?'bg-green-50 text-green-700 border-green-200':''}`}><Check size={14}/> Going</button><button disabled={busy} onClick={()=>choose('not_going')} className={`btn py-1.5 px-2.5 text-xs ${status==='not_going'?'bg-slate-100':''}`}><X size={14}/> Not Going</button></div></div>;
const going=attendance?.going||[];return <div className="mt-4 pt-4 border-t"><div className="flex items-center gap-2 text-sm font-bold"><Users size={15}/>{going.length} player{going.length===1?'':'s'} going</div>{going.length>0&&<div className="flex flex-wrap gap-2 mt-2">{going.map((p:any)=><span key={p.athleteUserId} className="pill">{p.name}</span>)}</div>}</div>}
