'use client';
import {useEffect} from 'react';
type MetricName='LCP'|'INP'|'CLS';
const budgets:Record<MetricName,number>={LCP:2500,INP:200,CLS:.1};
export default function WebVitalsMonitor(){useEffect(()=>{let route=location.pathname,cls=0,lcp=0,inp=0,sent=false;const observers:PerformanceObserver[]=[];const reset=()=>{route=location.pathname;cls=0;lcp=0;inp=0;sent=false};const send=(metric:MetricName,value:number)=>{if(!Number.isFinite(value)||value<0)return;const budget=budgets[metric],payload={type:'web-vital',metric,value,budget,overBudget:value>budget,route,ts:new Date().toISOString()};try{if(navigator.sendBeacon('/api/telemetry/vitals',new Blob([JSON.stringify(payload)],{type:'application/json'})))return}catch{}fetch('/api/telemetry/vitals',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(payload),keepalive:true}).catch(()=>{})};const flush=()=>{if(sent)return;sent=true;if(lcp>0)send('LCP',lcp);send('CLS',cls);if(inp>0)send('INP',inp)};const onVisibility=()=>{if(document.visibilityState==='hidden')flush()};const onPageHide=()=>flush();const onRoute=()=>{if(location.pathname===route)return;flush();reset()};document.addEventListener('visibilitychange',onVisibility);window.addEventListener('pagehide',onPageHide);window.addEventListener('popstate',onRoute);const originalPush=history.pushState,originalReplace=history.replaceState;history.pushState=function(...args){originalPush.apply(this,args as any);queueMicrotask(onRoute)} as typeof history.pushState;history.replaceState=function(...args){originalReplace.apply(this,args as any);queueMicrotask(onRoute)} as typeof history.replaceState;try{const po=new PerformanceObserver(list=>{for(const e of list.getEntries() as any[])if(!e.hadRecentInput)cls+=Number(e.value)||0});po.observe({type:'layout-shift',buffered:true} as any);observers.push(po)}catch{}try{const po=new PerformanceObserver(list=>{const entries=list.getEntries();const e=entries[entries.length-1] as any;if(e)lcp=Math.max(lcp,Number(e.startTime)||0)});po.observe({type:'largest-contentful-paint',buffered:true} as any);observers.push(po)}catch{}try{const po=new PerformanceObserver(list=>{for(const e of list.getEntries() as any[])inp=Math.max(inp,Number(e.duration)||0)});po.observe({type:'event',buffered:true,durationThreshold:40} as any);observers.push(po)}catch{}return()=>{flush();observers.forEach(o=>o.disconnect());document.removeEventListener('visibilitychange',onVisibility);window.removeEventListener('pagehide',onPageHide);window.removeEventListener('popstate',onRoute);history.pushState=originalPush;history.replaceState=originalReplace}},[]);return null}
