'use client';
import {useEffect,useState} from 'react';
export default function NetworkStatus(){const[online,setOnline]=useState(true),[reconnected,setReconnected]=useState(false);useEffect(()=>{setOnline(navigator.onLine);const on=()=>{setOnline(true);setReconnected(true);window.setTimeout(()=>setReconnected(false),2500)},off=()=>{setOnline(false);setReconnected(false)};window.addEventListener('online',on);window.addEventListener('offline',off);return()=>{window.removeEventListener('online',on);window.removeEventListener('offline',off)}},[]);if(online&&!reconnected)return null;return <div role="status" aria-live="polite" className={`rr-network-status ${online?'is-online':'is-offline'}`}>{online?'Back online. You can continue.':'You’re offline. Keep this page open; saved changes will resume when your connection returns.'}</div>}
