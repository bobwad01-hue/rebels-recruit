'use client';
import {useEffect,useMemo,useState} from 'react';
import AppShell from '@/components/AppShell';import PageHeader from '@/components/PageHeader';import {createClient} from '@/lib/supabase-browser';import {cleanDisplayNote} from '@/lib/display-notes';import {RECRUITING_JOURNEY,normalizeJourneyStage} from '@/lib/recruiting-journey';import {BarChart3,CalendarDays,CheckSquare,Download,Eye,FileSpreadsheet,Flag,Film,History,Network,School,Target,Users,X} from 'lucide-react';
type Report={id:string;title:string;description:string;ownerOnly?:boolean;multi?:boolean;icon:any};type Sheet={name:string;title:string;rows:Record<string,any>[]};
const one=(v:any)=>Array.isArray(v)?v[0]:v;const today=()=>new Date().toISOString().slice(0,10);const safe=(s:string)=>s.toLowerCase().replace(/[^a-z0-9]+/g,'-').replace(/^-|-$/g,'');const days=(d:any)=>d?Math.max(0,Math.floor((Date.now()-new Date(`${String(d).slice(0,10)}T12:00:00`).getTime())/86400000)):'';
const REPORTS:Report[]=[
{id:'weekly',title:'Advisor Weekly Report',description:'Players needing attention, overdue Next Moves, open tasks and recruiting priorities.',icon:CheckSquare},{id:'players',title:'Player Recruiting Summary',description:'One row per player with Connections, Journey, tasks, events and recruiting activity.',icon:Users},{id:'journey',title:'Recruiting Journey Report',description:'Current school-by-school stage, relationship activity and next move.',icon:Flag},{id:'coaches',title:'Coach Relationships',description:'Coach network, contact information, relationship stage and last contact.',icon:Network},{id:'communication',title:'Communication Activity',description:'Chronological recruiting interactions by player, school and coach.',icon:BarChart3},{id:'next_moves',title:'Next Moves & Tasks',description:'Athlete reminders and advisor-assigned tasks in one action report.',icon:CheckSquare},{id:'events',title:'Events & Attendance',description:'Camps, visits and recruiting events with Going / Not Going / Considering status.',icon:CalendarDays},{id:'videos',title:'Recruiting Video Library',description:'Recruiting videos by player, category, provider and link.',icon:Film},{id:'archived',title:'Archived / All-Time Relationships',description:'Stopped-pursuing schools with preserved recruiting history.',icon:History},{id:'organization',title:'Organization Recruiting Overview',description:'Executive player-by-player view of recruiting progress and workload.',ownerOnly:true,icon:Users},{id:'school_interest',title:'School Interest Report',description:'Schools attracting multiple organization players and their furthest stages.',ownerOnly:true,icon:School},{id:'outcomes',title:'Recruiting Outcomes / Commitments',description:'Committed players and programs.',ownerOnly:true,icon:Target},{id:'workbook',title:'Complete Organization Recruiting Report',description:'Multi-sheet workbook covering players, Connections, communication, Next Moves, events, videos and outcomes.',ownerOnly:true,multi:true,icon:FileSpreadsheet}];
export default function Exports(){const c=createClient();const[loading,setLoading]=useState(true),[busy,setBusy]=useState(''),[msg,setMsg]=useState(''),[role,setRole]=useState('advisor'),[orgName,setOrgName]=useState('Rebels'),[profiles,setProfiles]=useState<any[]>([]),[athleteProfiles,setAthleteProfiles]=useState<any[]>([]),[colleges,setColleges]=useState<any[]>([]),[coaches,setCoaches]=useState<any[]>([]),[interactions,setInteractions]=useState<any[]>([]),[reminders,setReminders]=useState<any[]>([]),[tasks,setTasks]=useState<any[]>([]),[events,setEvents]=useState<any[]>([]),[videos,setVideos]=useState<any[]>([]),[teams,setTeams]=useState<any[]>([]),[teamMembers,setTeamMembers]=useState<any[]>([]),[teamFilter,setTeamFilter]=useState('all'),[playerFilter,setPlayerFilter]=useState('all'),[yearFilter,setYearFilter]=useState('all'),[divisionFilter,setDivisionFilter]=useState('all'),[stageFilter,setStageFilter]=useState('all'),[activityFilter,setActivityFilter]=useState('all'),[eventStatusFilter,setEventStatusFilter]=useState('all'),[scope,setScope]=useState<'current'|'all'>('current'),[dateFrom,setDateFrom]=useState(''),[dateTo,setDateTo]=useState(''),[preview,setPreview]=useState<{report:Report;sheets:Sheet[]}|null>(null),[sheetIndex,setSheetIndex]=useState(0);
useEffect(()=>{(async()=>{const{data:{user}}=await c.auth.getUser();if(!user){setLoading(false);return}const{data:me}=await c.from('organization_members').select('organization_id,role,organization_view_access,organizations(name)').eq('user_id',user.id).eq('status','active').maybeSingle();if(!me){setLoading(false);return}setRole(me.role);setOrgName(one(me.organizations)?.name||'Rebels');const{data:members}=await c.from('organization_members').select('user_id,role').eq('organization_id',me.organization_id).eq('status','active');const{data:assign}=await c.from('athlete_advisor_assignments').select('athlete_user_id').eq('advisor_user_id',user.id).eq('status','active');const all=(members||[]).filter((m:any)=>m.role==='athlete').map((m:any)=>m.user_id),ids=(me.role==='owner'||me.role==='admin'||me.organization_view_access)?all:(assign||[]).map((a:any)=>a.athlete_user_id);if(!ids.length){setLoading(false);return}const qs=[c.from('profiles').select('id,full_name,email').in('id',ids),c.from('athlete_profiles').select('*').in('user_id',ids),c.from('athlete_colleges').select('id,athlete_user_id,college_id,status,archived_at,archived_reason,created_at,colleges(id,name,division,state,city,conference)').in('athlete_user_id',ids),c.from('athlete_coaches').select('id,athlete_user_id,coach_id,college_id,next_step,last_contact_date,archived_at,college_coaches(id,first_name,last_name,title,email,phone),colleges(id,name,division)').in('athlete_user_id',ids),c.from('interactions').select('id,athlete_user_id,college_id,coach_id,type,initiated_by,date,date_precision,date_year,date_month,note,created_at,colleges(name,division),college_coaches(first_name,last_name)').in('athlete_user_id',ids).order('created_at',{ascending:false}).limit(10000),c.from('reminders').select('id,athlete_user_id,title,due_date,status,colleges(name),college_coaches(first_name,last_name)').in('athlete_user_id',ids),c.from('advisor_tasks').select('id,athlete_user_id,title,description,due_date,status,created_at').in('athlete_user_id',ids),c.from('athlete_events').select('athlete_user_id,status,notes,events(id,name,type,date,location,registration_url,colleges(name,division))').in('athlete_user_id',ids),c.from('athlete_videos').select('id,athlete_user_id,title,category,provider,url,created_at').in('athlete_user_id',ids)];const[r1,r2,r3,r4,r5,r6,r7,r8,r9]=await Promise.all(qs);setProfiles(r1.data||[]);setAthleteProfiles(r2.data||[]);setColleges(r3.data||[]);setCoaches(r4.data||[]);setInteractions(r5.data||[]);setReminders(r6.data||[]);setTasks(r7.data||[]);setEvents(r8.data||[]);setVideos(r9.data||[]);try{const tr=await fetch('/api/profile/team/organization',{cache:'no-store'});if(tr.ok){const td=await tr.json();setTeams(td.teams||[]);setTeamMembers(td.members||[])}}catch{}setLoading(false)})()},[]);
const owner=role==='owner'||role==='admin',pm=useMemo(()=>new Map(profiles.map(p=>[p.id,p])),[profiles]),am=useMemo(()=>new Map(athleteProfiles.map(p=>[p.user_id,p])),[athleteProfiles]),tm=useMemo(()=>new Map(teams.map(t=>[t.id,t.name])),[teams]),atm=useMemo(()=>{const m=new Map<string,string>();teamMembers.forEach(x=>m.set(x.athlete_user_id,tm.get(x.team_id)||''));return m},[teamMembers,tm]);const name=(id:string)=>pm.get(id)?.full_name||pm.get(id)?.email||'Player',team=(id:string)=>atm.get(id)||'Unassigned';const years=[...new Set(athleteProfiles.map(a=>String(a.class_year||'')).filter(Boolean))].sort(),divisions=[...new Set(colleges.map(x=>String(one(x.colleges)?.division||'')).filter(Boolean))].sort(),activityTypes=[...new Set(interactions.map(x=>String(x.type||'')).filter(Boolean))].sort();const ids=useMemo(()=>athleteProfiles.map(a=>a.user_id).filter(id=>(teamFilter==='all'||team(id)===teamFilter)&&(playerFilter==='all'||id===playerFilter)&&(yearFilter==='all'||String(am.get(id)?.class_year||'')===yearFilter)),[athleteProfiles,teamFilter,playerFilter,yearFilter,atm,am]),idset=new Set(ids);const inDate=(v:any)=>{const d=String(v||'').slice(0,10);return(!dateFrom||!d||d>=dateFrom)&&(!dateTo||!d||d<=dateTo)},schoolOK=(x:any)=>idset.has(x.athlete_user_id)&&(scope==='all'||!x.archived_at)&&(divisionFilter==='all'||String(one(x.colleges)?.division||'')===divisionFilter)&&(stageFilter==='all'||normalizeJourneyStage(x.status)===stageFilter);
function playerRows(){return ids.map(id=>{const cs=colleges.filter(x=>x.athlete_user_id===id&&!x.archived_at),cc=coaches.filter(x=>x.athlete_user_id===id&&!x.archived_at),ii=interactions.filter(x=>x.athlete_user_id===id&&x.date_precision==='exact'&&x.date),rr=reminders.filter(x=>x.athlete_user_id===id&&x.status!=='completed'),tt=tasks.filter(x=>x.athlete_user_id===id&&x.status!=='completed'),ee=events.filter(x=>x.athlete_user_id===id&&one(x.events)?.date>=today()),counts=Object.fromEntries(RECRUITING_JOURNEY.map(s=>[s,cs.filter(x=>normalizeJourneyStage(x.status)===s).length])),last=ii.map(x=>x.date).sort().reverse()[0];return{'Player':name(id),'Team':team(id),'Class Year':am.get(id)?.class_year||'','Active Schools':cs.length,'Coach Relationships':cc.length,'Target Schools':counts['Target School'],'Engaged':counts['Engaged'],'Interested':counts['Interested'],'Visit/Camp':counts['Visit/Camp'],'Offers':counts['Offer'],'Committed':counts['Committed'],'Open Reminders':rr.length,'Open Advisor Tasks':tt.length,'Upcoming Events':ee.length,'Last Exact Activity':last||'','Days Since Exact Activity':days(last)}})}
function journeyRows(){return colleges.filter(schoolOK).map(x=>{const s=one(x.colleges)||{},ii=interactions.filter(i=>i.athlete_user_id===x.athlete_user_id&&i.college_id===x.college_id&&i.date_precision==='exact'&&i.date),last=ii.map(i=>i.date).sort().reverse()[0],next=coaches.find(c=>c.athlete_user_id===x.athlete_user_id&&c.college_id===x.college_id&&!c.archived_at)?.next_step||'';return{'Player':name(x.athlete_user_id),'Team':team(x.athlete_user_id),'School':s.name||'','Division':s.division||'','Conference':s.conference||'','State':s.state||'','Journey Stage':normalizeJourneyStage(x.status),'Last Exact Contact':last||'','Days Since Contact':days(last),'Next Move':next,'Archived':x.archived_at?'Yes':'No'}})}
function coachRows(){return coaches.filter(x=>idset.has(x.athlete_user_id)&&(scope==='all'||!x.archived_at)&&(divisionFilter==='all'||String(one(x.colleges)?.division||'')===divisionFilter)).map(x=>{const c=one(x.college_coaches)||{},s=one(x.colleges)||{},rel=colleges.find(r=>r.athlete_user_id===x.athlete_user_id&&r.college_id===x.college_id);return{'Player':name(x.athlete_user_id),'Team':team(x.athlete_user_id),'School':s.name||'','Journey Stage':normalizeJourneyStage(rel?.status),'Coach':`${c.first_name||''} ${c.last_name||''}`.trim(),'Title':c.title||'','Email':c.email||'','Phone':c.phone||'','Last Contact':x.last_contact_date||'','Next Move':x.next_step||''}})}
function communicationRows(){return interactions.filter(x=>idset.has(x.athlete_user_id)&&inDate(x.date||x.created_at)&&(activityFilter==='all'||x.type===activityFilter)&&(divisionFilter==='all'||String(one(x.colleges)?.division||'')===divisionFilter)).map(x=>({'Date':x.date_precision==='exact'&&x.date?x.date:x.date_precision==='month'?`${x.date_year}-${String(x.date_month||'').padStart(2,'0')}`:x.date_precision==='year'?x.date_year||'':'Unknown','Date Precision':x.date_precision||'exact','Player':name(x.athlete_user_id),'Team':team(x.athlete_user_id),'School':one(x.colleges)?.name||'','Coach':`${one(x.college_coaches)?.first_name||''} ${one(x.college_coaches)?.last_name||''}`.trim(),'Type':x.type||'','Initiated By':x.initiated_by||'','Notes':cleanDisplayNote(x.note)}))}
function nextMoveRows(){const out:Record<string,any>[]=[];reminders.filter(x=>idset.has(x.athlete_user_id)&&inDate(x.due_date)).forEach(x=>out.push({'Player':name(x.athlete_user_id),'Team':team(x.athlete_user_id),'Source':'Athlete Reminder','Title':x.title,'Description':'','Due Date':x.due_date||'','Status':x.status||'','School':one(x.colleges)?.name||'','Coach':`${one(x.college_coaches)?.first_name||''} ${one(x.college_coaches)?.last_name||''}`.trim()}));tasks.filter(x=>idset.has(x.athlete_user_id)&&inDate(x.due_date||x.created_at)).forEach(x=>out.push({'Player':name(x.athlete_user_id),'Team':team(x.athlete_user_id),'Source':'Advisor Task','Title':x.title,'Description':x.description||'','Due Date':x.due_date||'','Status':x.status||'','School':'','Coach':''}));return out}
function eventRows(){return events.filter(x=>idset.has(x.athlete_user_id)&&inDate(one(x.events)?.date)&&(eventStatusFilter==='all'||x.status===eventStatusFilter)).map(x=>{const e=one(x.events)||{};return{'Player':name(x.athlete_user_id),'Team':team(x.athlete_user_id),'Date':e.date||'','Event':e.name||'','Type':e.type||'','School':one(e.colleges)?.name||'','Location':e.location||'','Attendance':x.status||'Considering','Registration':e.registration_url||'','Notes':cleanDisplayNote(x.notes)}})}
function videoRows(){return videos.filter(x=>idset.has(x.athlete_user_id)&&inDate(x.created_at)).map(x=>({'Player':name(x.athlete_user_id),'Team':team(x.athlete_user_id),'Added':String(x.created_at||'').slice(0,10),'Title':x.title||'','Category':x.category||'','Provider':x.provider||'','Video URL':x.url||''}))}
function archivedRows(){return colleges.filter(x=>idset.has(x.athlete_user_id)&&x.archived_at).map(x=>({'Player':name(x.athlete_user_id),'Team':team(x.athlete_user_id),'School':one(x.colleges)?.name||'','Division':one(x.colleges)?.division||'','Stage at Archive':normalizeJourneyStage(x.status),'Archived Date':String(x.archived_at).slice(0,10),'Reason':cleanDisplayNote(x.archived_reason)}))}
function schoolInterestRows(){const m=new Map<string,any>();colleges.filter(schoolOK).forEach(x=>{const s=one(x.colleges)||{},k=x.college_id||s.name,v=m.get(k)||{school:s.name,division:s.division,players:[],stages:[]};v.players.push(name(x.athlete_user_id));v.stages.push(normalizeJourneyStage(x.status));m.set(k,v)});return[...m.values()].map(v=>({'School':v.school,'Division':v.division,'Players Pursuing':v.players.length,'Players':v.players.join(', '),'Stages':v.stages.join(', ')})).sort((a,b)=>Number(b['Players Pursuing'])-Number(a['Players Pursuing']))}
function outcomeRows(){return colleges.filter(x=>idset.has(x.athlete_user_id)&&normalizeJourneyStage(x.status)==='Committed').map(x=>({'Player':name(x.athlete_user_id),'Team':team(x.athlete_user_id),'Class Year':am.get(x.athlete_user_id)?.class_year||'','School':one(x.colleges)?.name||'','Division':one(x.colleges)?.division||''}))}
function weeklyRows(){return playerRows().map(x=>({...x,'Suggested Focus':Number(x['Open Advisor Tasks'])+Number(x['Open Reminders'])>0?'Complete open Next Moves':x['Last Exact Activity']===''||Number(x['Days Since Exact Activity'])>=14?'Review relationships needing attention':Number(x['Active Schools'])===0?'Build Connections':'Keep recruiting momentum'}))}
function sheets(k:string):Sheet[]{const oneSheet=(name:string,title:string,rows:any[]):Sheet[]=>[{name,title,rows}];if(k==='weekly')return oneSheet('Weekly','Advisor Weekly Report',weeklyRows());if(k==='players'||k==='organization')return oneSheet('Players',k==='organization'?'Organization Recruiting Overview':'Player Recruiting Summary',playerRows());if(k==='journey')return oneSheet('Connections','Recruiting Journey Report',journeyRows());if(k==='coaches')return oneSheet('Coaches','Coach Relationships',coachRows());if(k==='communication')return oneSheet('Communication','Communication Activity',communicationRows());if(k==='next_moves')return oneSheet('Next Moves','Next Moves & Tasks',nextMoveRows());if(k==='events')return oneSheet('Events','Events & Attendance',eventRows());if(k==='videos')return oneSheet('Videos','Recruiting Video Library',videoRows());if(k==='archived')return oneSheet('Archived','Archived / All-Time Relationships',archivedRows());if(k==='school_interest')return oneSheet('School Interest','School Interest Report',schoolInterestRows());if(k==='outcomes')return oneSheet('Commitments','Recruiting Outcomes / Commitments',outcomeRows());if(k==='workbook')return[['Players','Player Overview',playerRows()],['Connections','Connections & Journey',journeyRows()],['Coaches','Coach Relationships',coachRows()],['Communication','Communication Activity',communicationRows()],['Next Moves','Next Moves & Tasks',nextMoveRows()],['Events','Events & Attendance',eventRows()],['Videos','Recruiting Videos',videoRows()],['Commitments','Recruiting Outcomes',outcomeRows()]].map(([name,title,rows]:any)=>({name,title,rows}));return[]}
function csv(filename:string,rows:any[]){if(!rows.length)return;const keys=Object.keys(rows[0]),q=(v:any)=>`"${String(v??'').replaceAll('"','""')}"`,blob=new Blob(['\ufeff'+[keys.map(q).join(','),...rows.map(r=>keys.map(k=>q(r[k])).join(','))].join('\n')],{type:'text/csv;charset=utf-8'}),a=document.createElement('a');a.href=URL.createObjectURL(blob);a.download=filename;a.click();setTimeout(()=>URL.revokeObjectURL(a.href),500)}
async function excel(kind:string,ss:Sheet[]){const XLSX=await import('xlsx'),wb=XLSX.utils.book_new();ss.forEach(s=>{const h=s.rows.length?Object.keys(s.rows[0]):['No data'],data=s.rows.length?s.rows.map(r=>h.map(k=>r[k]??'')):[['No matching data.']],aoa:any[][]=[[s.title],[`${orgName} • Generated ${new Date().toLocaleString()}`],[],h,...data],ws=XLSX.utils.aoa_to_sheet(aoa);ws['!cols']=h.map(k=>({wch:Math.min(44,Math.max(12,k.length+2,...s.rows.slice(0,50).map(r=>String(r[k]??'').length+2)))}));ws['!autofilter']={ref:`A4:${XLSX.utils.encode_col(Math.max(0,h.length-1))}${aoa.length}`};XLSX.utils.book_append_sheet(wb,ws,s.name.slice(0,31))});XLSX.writeFile(wb,`rebels-recruit-${safe(REPORTS.find(r=>r.id===kind)?.title||kind)}-${today()}.xlsx`,{compression:true})}
async function run(kind:string,format:'xlsx'|'csv'){setBusy(`${kind}-${format}`);setMsg('');const ss=sheets(kind),count=ss.reduce((n,s)=>n+s.rows.length,0);if(!count){setMsg('No matching data for this report and filter combination.');setBusy('');return}try{if(format==='xlsx')await excel(kind,ss);else ss.forEach(s=>csv(`rebels-recruit-${safe(s.name)}-${today()}.csv`,s.rows));setMsg('Report prepared successfully.')}catch(e){setMsg(e instanceof Error?e.message:'Could not generate report.')}setBusy('')}
if(loading)return <AppShell><div className="max-w-6xl mx-auto px-5 py-8"><div className="card p-10 text-center muted">Building your Report Center...</div></div></AppShell>;const visible=REPORTS.filter(r=>!r.ownerOnly||owner),active=preview?.sheets[sheetIndex]||preview?.sheets[0],headers=active?.rows.length?Object.keys(active.rows[0]):[];const reset=()=>{setTeamFilter('all');setPlayerFilter('all');setYearFilter('all');setDivisionFilter('all');setStageFilter('all');setActivityFilter('all');setEventStatusFilter('all');setScope('current');setDateFrom('');setDateTo('')};return <AppShell><div className="max-w-6xl mx-auto px-4 sm:px-5 md:px-8 py-6"><PageHeader title="Exports & Reports" subtitle={owner?'Create recruiting reports across your organization.':'Create recruiting reports for the players you advise.'}/><section className="card p-5 mb-6"><div className="flex items-center gap-2"><FileSpreadsheet size={19}/><h2 className="font-black text-lg">Report Filters</h2></div><p className="muted text-sm mt-1">Filters apply where relevant. Historical import metadata is automatically removed from all report-facing notes.</p><div className="grid sm:grid-cols-2 lg:grid-cols-4 gap-3 mt-4"><Select label="Team" value={teamFilter} set={setTeamFilter} options={[['all','All Teams'],...teams.map(t=>[t.name,t.name])]}/><Select label="Player" value={playerFilter} set={setPlayerFilter} options={[['all','All Players'],...athleteProfiles.map(a=>[a.user_id,name(a.user_id)])]}/><Select label="Class Year" value={yearFilter} set={setYearFilter} options={[['all','All Class Years'],...years.map(x=>[x,x])]}/><Select label="Division" value={divisionFilter} set={setDivisionFilter} options={[['all','All Divisions'],...divisions.map(x=>[x,x])]}/><Select label="Journey Stage" value={stageFilter} set={setStageFilter} options={[['all','All Stages'],...RECRUITING_JOURNEY.map(x=>[x,x])]}/><Select label="Activity Type" value={activityFilter} set={setActivityFilter} options={[['all','All Activity Types'],...activityTypes.map(x=>[x,x])]}/><Select label="Event Attendance" value={eventStatusFilter} set={setEventStatusFilter} options={[['all','All Attendance'],['going','Going'],['considering','Considering'],['not_going','Not Going']]}/><Select label="Relationship Scope" value={scope} set={v=>setScope(v as any)} options={[['current','Current'],['all','All-Time']]}/><label className="text-sm font-bold">From<input type="date" className="input mt-1" value={dateFrom} onChange={e=>setDateFrom(e.target.value)}/></label><label className="text-sm font-bold">Through<input type="date" className="input mt-1" value={dateTo} onChange={e=>setDateTo(e.target.value)}/></label><div className="flex items-end"><button className="btn w-full" onClick={reset}>Reset Filters</button></div></div></section>{msg&&<div className="mb-5 rounded-xl border bg-white px-4 py-3 text-sm font-semibold">{msg}</div>}<div className="grid md:grid-cols-2 gap-4">{visible.map(r=>{const I=r.icon;return <section key={r.id} className={`card p-5 flex flex-col ${r.multi?'md:col-span-2':''}`}><div className="flex gap-3"><div className="h-10 w-10 rounded-xl bg-slate-100 flex items-center justify-center"><I size={20}/></div><div><h3 className="font-black text-lg">{r.title}</h3><p className="muted text-sm mt-1">{r.description}</p></div></div><div className="mt-5 flex gap-2 flex-wrap"><button className="btn" onClick={()=>{setSheetIndex(0);setPreview({report:r,sheets:sheets(r.id)})}}><Eye size={16}/>Preview</button><button className="btn btn-red" disabled={!!busy} onClick={()=>run(r.id,'xlsx')}><FileSpreadsheet size={16}/>Download Excel</button><button className="btn" disabled={!!busy} onClick={()=>run(r.id,'csv')}><Download size={16}/>CSV</button></div></section>})}</div></div>{preview&&<div className="fixed inset-0 z-50 bg-slate-950/50 flex items-center justify-center p-3" onMouseDown={e=>{if(e.currentTarget===e.target)setPreview(null)}}><div className="bg-white rounded-2xl shadow-2xl w-full max-w-[96vw] h-[88vh] flex flex-col overflow-hidden"><div className="px-5 py-4 border-b flex justify-between"><div><h2 className="text-xl font-black">{preview.report.title} Preview</h2><p className="muted text-sm">Current filters · review before downloading.</p></div><button onClick={()=>setPreview(null)}><X/></button></div>{preview.sheets.length>1&&<div className="px-5 pt-3 flex gap-2 overflow-x-auto border-b">{preview.sheets.map((s,i)=><button key={s.name} className={`px-3 py-2 text-sm font-bold border-b-2 ${sheetIndex===i?'border-red-600 text-red-700':'border-transparent'}`} onClick={()=>setSheetIndex(i)}>{s.name} ({s.rows.length})</button>)}</div>}<div className="px-5 py-3 border-b flex justify-between gap-3"><div><b>{active?.title}</b><div className="muted text-xs">{active?.rows.length||0} matching rows{(active?.rows.length||0)>50?' · first 50 shown':''}</div></div><div className="flex gap-2"><button className="btn btn-red" onClick={()=>run(preview.report.id,'xlsx')}>Excel</button><button className="btn" onClick={()=>run(preview.report.id,'csv')}>CSV</button></div></div><div className="flex-1 overflow-auto">{active?.rows.length?<table className="min-w-max w-full text-sm"><thead className="sticky top-0 bg-slate-100"><tr>{headers.map(h=><th key={h} className="text-left font-black px-4 py-3 border-b">{h}</th>)}</tr></thead><tbody>{active.rows.slice(0,50).map((r,i)=><tr key={i} className="border-b">{headers.map(h=><td key={h} className="px-4 py-3 max-w-[360px] whitespace-pre-wrap">{String(r[h]??'')}</td>)}</tr>)}</tbody></table>:<div className="p-10 text-center muted">No matching data.</div>}</div></div></div>}</AppShell>}
function Select({label,value,set,options}:{label:string;value:string;set:(v:string)=>void;options:any[]}){return <label className="text-sm font-bold">{label}<select className="input mt-1" value={value} onChange={e=>set(e.target.value)}>{options.map(([v,l])=><option key={v} value={v}>{l}</option>)}</select></label>}
