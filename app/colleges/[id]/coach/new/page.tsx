'use client';import {useState} from 'react';import {useParams} from 'next/navigation';import Link from 'next/link';import AppShell from '@/components/AppShell';import {createClient} from '@/lib/supabase-browser';
export default function NewCoach(){const {id}=useParams<{id:string}>();const [first,setFirst]=useState('');const [last,setLast]=useState('');const [title,setTitle]=useState('');const [email,setEmail]=useState('');const [phone,setPhone]=useState('');const [x,setX]=useState('');const [instagram,setInstagram]=useState('');const [msg,setMsg]=useState('');async function save(e:React.FormEvent){e.preventDefault();setMsg('');const {error}=await createClient().from('college_coaches').insert({college_id:id,first_name:first,last_name:last,title,email,phone,x_url:x,instagram_url:instagram});if(error){setMsg(error.message);return}location.href=`/colleges/${id}`}return <AppShell><div className="max-w-xl mx-auto px-5 md:px-8 py-8"><Link href={`/colleges/${id}`} className="text-sm font-bold">← Back to college</Link><div className="card p-6 mt-5"><h1 className="text-2xl font-black">Add a coach</h1><p className="muted mt-1">Create the coach relationship once, then use it throughout the recruiting timeline.</p><form onSubmit={save} className="space-y-4 mt-7"><div className="grid grid-cols-2 gap-4"><input className="input" placeholder="First name" value={first} onChange={e=>setFirst(e.target.value)} required/><input className="input" placeholder="Last name" value={last} onChange={e=>setLast(e.target.value)} required/></div><select className="input" value={title} onChange={e=>setTitle(e.target.value)}><option value="">Select title</option><option value="Head Coach">Head Coach</option><option value="Assistant">Assistant</option><option value="Recruiting Coordinator">Recruiting Coordinator</option><option value="Other">Other</option></select><input className="input" type="email" placeholder="Email" value={email} onChange={e=>setEmail(e.target.value)}/><input className="input" placeholder="Phone" value={phone} onChange={e=>setPhone(e.target.value)}/><input className="input" placeholder="X/Twitter URL" value={x} onChange={e=>setX(e.target.value)}/><input className="input" placeholder="Instagram URL" value={instagram} onChange={e=>setInstagram(e.target.value)}/>{msg&&<p className="text-sm text-red-600">{msg}</p>}<button className="btn btn-red w-full">Save coach</button></form></div></div></AppShell>}
