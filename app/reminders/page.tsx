'use client';import {useEffect,useState} from 'react';import AppShell from '@/components/AppShell';import PageHeader from '@/components/PageHeader';import {createClient} from '@/lib/supabase-browser';
export default function Reminders(){const [rows,setRows]=useState<any[]>([]);async function load(){const c=createClient();const {data:{user}}=await c.auth.getUser();const {data}=await c.from('reminders').select('*,colleges(name)').eq('owner_user_id',user?.id).order('due_date');setRows(data||[])}useEffect(()=>{load()},[]);async function complete(id:string){await createClient().from('reminders').update({status:'completed'}).eq('id',id);load()}return <AppShell><div className="max-w-5xl mx-auto px-5 md:px-8 py-6"><PageHeader title="Reminders" subtitle="The next action should always be obvious."/><div className="space-y-3">{rows.map(r=><div className="card p-5 flex items-center gap-4" key={r.id}><input type="checkbox" checked={r.status==='completed'} onChange={()=>complete(r.id)}/><div className="flex-1"><div className="font-bold">{r.title}</div><div className="muted text-sm mt-1">{r.colleges?.name||'Recruiting'} · Due {r.due_date}</div></div><span className="pill">{r.status}</span></div>)}{!rows.length&&<div className="card p-12 text-center muted">No reminders. You’re caught up.</div>}</div></div></AppShell>}
