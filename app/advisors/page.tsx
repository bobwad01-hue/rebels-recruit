'use client';
import Link from 'next/link';import {useEffect,useMemo,useState} from 'react';import {AlertCircle,ArrowRight,BarChart3,Clock3,MessageSquare,Plus,Search,UserPlus} from 'lucide-react';import AppShell from '@/components/AppShell';import PageHeader from '@/components/PageHeader';import {createClient} from '@/lib/supabase-browser';

type Player={id:string;name:string;email:string;class_year?:number;school_name?:string;positions?:string[];gpa?:number;status:string;relationship_type?:string;collegeCount:number;coachCount:number;interactionCount:number;openFollowups:number;overdue:number;lastActivity:string|null;};
const statusLabel=(s:string)=>s==='pending'?'Pending invite':s==='active'?'Active':s==='declined'?'Declined':s==='revoked'?'Access revoked':s==='suspended'?'Suspended':s;
export default function Advisors(){const c=createClient();const [role,setRole]=useState('');const [orgId,setOrgId]=useState('');const [userId,setUserId]=useState('');const [players,setPlayers]=useState<Player[]>([]);const [advisors,setAdvisors]=useState<any[]>([]);const [assignments,setAssignments]=useState<any[]>([]);const [search,setSearch]=useState('');const [classFilter,setClassFilter]=useState('all');const [statusFilter,setStatusFilter]=useState('all');const [selected,setSelected]=useState<string[]>([]);const [selectedAdvisor,setSelectedAdvisor]=useState('');const [selectedPlayer,setSelectedPlayer]=useState('');const [relationshipType,setRelationshipType]=useState('Recruiting Advisor');const [msg,setMsg]=useState('');const [loading,setLoading]=useState(true);
async function load(){setLoading(true);const {data:{user}}=await c.auth.getUser();if(!user){setLoading(false);return}setUserId(user.id);const {data:me}=await c.from('organization_members').select('organization_id,role').eq('user_id',user.id).eq('status','active').single();if(!me){setLoading(false);return}setRole(me.role);setOrgId(me.organization_id);const {data:members}=await c.from('organization_members').select('user_id,role,status').eq('organization_id',me.organization_id).eq('status','active');const ids=(members||[]).map((x:any)=>x.user_id);const {data:profiles}=ids.length?await c.from('profiles').select('id,full_name,email').in('id',ids):{data:[]};const pm=new Map((profiles||[]).map((p:any)=>[p.id,p]));setAdvisors((members||[]).filter((m:any)=>['owner','admin','advisor'].includes(m.role)).map((m:any)=>({...m,profile:pm.get(m.user_id)})));const {data:as}=await c.from('athlete_advisor_assignments').select('*').eq('organization_id',me.organization_id);setAssignments(as||[]);const candidateIds=me.role==='owner'||me.role==='admin'?((members||[]).filter((m:any)=>m.role==='athlete').map((m:any)=>m.user_id)):((as||[]).filter((a:any)=>a.advisor_user_id===user.id&&a.status==='active').map((a:any)=>a.athlete_user_id));const unique=[...new Set(candidateIds)];if(!unique.length){setPlayers([]);setLoading(false);return}const [aps,acs,coachRows,intRows,remRows]=await Promise.all([c.from('athlete_profiles').select('user_id,class_year,school_name,positions,gpa').in('user_id',unique),c.from('athlete_colleges').select('athlete_user_id,id').in('athlete_user_id',unique),c.from('athlete_coaches').select('athlete_user_id,id').in('athlete_user_id',unique),c.from('interactions').select('athlete_user_id,id,date,created_at').in('athlete_user_id',unique).order('date',{ascending:false}),c.from('reminders').select('athlete_user_id,id,due_date,status').in('athlete_user_id',unique).eq('status','open')]);const apm=new Map((aps.data||[]).map((x:any)=>[x.user_id,x]));const count=(rows:any[],id:string)=>rows.reduce((m:any,r:any)=>(m[r[id]]=(m[r[id]]||0)+1,m),{});const cc=count(acs.data||[],'athlete_user_id'),co=count(coachRows.data||[],'athlete_user_id'),ii=count(intRows.data||[],'athlete_user_id'),rr=count(remRows.data||[],'athlete_user_id');const now=new Date().toISOString().slice(0,10);const byLast=new Map<string,string>();(intRows.data||[]).forEach((r:any)=>{if(!byLast.has(r.athlete_user_id))byLast.set(r.athlete_user_id,r.date||r.created_at?.slice(0,10)||'')});const asMap=new Map<string,any>();(as||[]).filter((a:any)=>a.advisor_user_id===user.id).forEach((a:any)=>asMap.set(a.athlete_user_id,a));setPlayers(unique.map(id=>{const p=pm.get(id)||{};const ap=apm.get(id)||{};const pending=asMap.get(id);const rem=(remRows.data||[]).filter((r:any)=>r.athlete_user_id===id);return{id,name:p.full_name||'Athlete',email:p.email||'',...ap,status:pending?.status||'active',relationship_type:pending?.relationship_type,collegeCount:cc[id]||0,coachCount:co[id]||0,interactionCount:ii[id]||0,openFollowups:rr[id]||0,overdue:rem.filter((r:any)=>r.due_date<now).length,lastActivity:byLast.get(id)||null};}));setLoading(false)}
useEffect(()=>{load()},[]);
const filtered=useMemo(()=>players.filter(p=>(p.name+' '+p.email+' '+(p.school_name||'')).toLowerCase().includes(search.toLowerCase())&&(classFilter==='all'||String(p.class_year)===classFilter)&&(statusFilter==='all'||p.status===statusFilter)),[players,search,classFilter,statusFilter]);
const stats=useMemo(()=>({players:players.filter(p=>p.status==='active').length,colleges:players.reduce((n,p)=>n+p.collegeCount,0),coaches:players.reduce((n,p)=>n+p.coachCount,0),interactions:players.reduce((n,p)=>n+p.interactionCount,0),followups:players.reduce((n,p)=>n+p.openFollowups,0),overdue:players.reduce((n,p)=>n+p.overdue,0),stale:players.filter(p=>!p.lastActivity||((Date.now()-new Date(p.lastActivity).getTime())/86400000)>14).length}),[players]);
const compare=players.filter(p=>selected.includes(p.id));
async function invite(){if(!selectedPlayer||!selectedAdvisor)return;const {error}=await c.from('athlete_advisor_assignments').insert({athlete_user_id:selectedPlayer,advisor_user_id:selectedAdvisor,organization_id:orgId,status:'pending',relationship_type:relationshipType});if(error)setMsg(error.message);else{setMsg('Invitation sent. The player must accept before the advisor can see any recruiting data.');setSelectedPlayer('');setSelectedAdvisor('');await load()}}
async function cancelInvite(id:string){const {error}=await c.from('athlete_advisor_assignments').delete().eq('id',id);setMsg(error?error.message:'Invitation cancelled.');await load()}
return <AppShell><div className="max-w-7xl mx-auto px-5 md:px-8 py-6"><PageHeader title="Advisor View" subtitle="Manage your players, relationships, recruiting activity and follow-ups." action={<Link href="/messages" className="btn btn-red"><MessageSquare size={17}/> Messages</Link>}/><div className="grid grid-cols-2 lg:grid-cols-6 gap-3"><div className="card p-4"><div className="muted text-xs">My Players</div><div className="font-black text-2xl mt-1">{stats.players}</div></div><div className="card p-4"><div className="muted text-xs">College Relationships</div><div className="font-black text-2xl mt-1">{stats.colleges}</div></div><div className="card p-4"><div className="muted text-xs">Coach Relationships</div><div className="font-black text-2xl mt-1">{stats.coaches}</div></div><div className="card p-4"><div className="muted text-xs">Interactions</div><div className="font-black text-2xl mt-1">{stats.interactions}</div></div><div className="card p-4"><div className="muted text-xs">Follow-ups Due</div><div className="font-black text-2xl mt-1">{stats.followups}</div></div><div className="card p-4"><div className="muted text-xs">Overdue</div><div className="font-black text-2xl mt-1 text-red-600">{stats.overdue}</div></div></div><div className="grid lg:grid-cols-3 gap-6 mt-6"><section className="card p-5 lg:col-span-2"><div className="flex items-center justify-between gap-3"><div><h2 className="font-black text-lg">Needs Attention</h2><p className="muted text-sm">Players or relationships that may need your attention.</p></div><AlertCircle size={20}/></div><div className="grid md:grid-cols-3 gap-3 mt-4"><div className="rounded-xl bg-red-50 p-4"><div className="font-black text-2xl">{stats.overdue}</div><div className="text-sm mt-1">Overdue follow-ups</div></div><div className="rounded-xl bg-amber-50 p-4"><div className="font-black text-2xl">{stats.stale}</div><div className="text-sm mt-1">No activity in 14+ days</div></div><div className="rounded-xl bg-slate-50 p-4"><div className="font-black text-2xl">{assignments.filter(a=>a.status==='pending').length}</div><div className="text-sm mt-1">Pending invitations</div></div></div></section><section className="card p-5"><h2 className="font-black text-lg">Recruiting Pipeline</h2><p className="muted text-sm mt-1">Open a player to review college stages.</p><div className="mt-4 space-y-2">{['Researching','Target','Contacted','Engaged','Recruiting Interest','High Interest','Visit/Camp','Decision','Committed'].map(stage=><div key={stage} className="flex items-center justify-between text-sm"><span>{stage}</span><span className="font-bold">—</span></div>)}</div></section></div>{(role==='owner'||role==='admin'||role==='advisor')&&<section className="card p-5 mt-6"><div className="flex items-center gap-2"><UserPlus size={19}/><div><h2 className="font-black text-lg">Invite an Advisor to a Player</h2><p className="muted text-sm">Invites create a pending relationship. Player acceptance is required before any data is shared.</p></div></div><div className="grid md:grid-cols-4 gap-3 mt-4"><select className="input" value={selectedPlayer} onChange={e=>setSelectedPlayer(e.target.value)}><option value="">Choose player</option>{players.map(p=><option key={p.id} value={p.id}>{p.name}</option>)}</select><select className="input" value={selectedAdvisor} onChange={e=>setSelectedAdvisor(e.target.value)}><option value="">Choose advisor</option>{advisors.map(a=><option key={a.user_id} value={a.user_id}>{a.profile?.full_name||a.profile?.email} · {a.role}</option>)}</select><select className="input" value={relationshipType} onChange={e=>setRelationshipType(e.target.value)}><option>Recruiting Advisor</option><option>Primary Advisor</option><option>Secondary Advisor</option><option>Recruiting Coach</option><option>Other</option></select><button className="btn btn-red" onClick={invite}><Plus size={17}/> Send Invite</button></div>{msg&&<p className="text-sm mt-3">{msg}</p>}</section>}<section className="card p-5 mt-6"><div className="flex flex-col md:flex-row md:items-center gap-3 justify-between"><div><h2 className="font-black text-lg">My Players</h2><p className="muted text-sm">Select one or more players to compare recruiting activity.</p></div><div className="flex flex-wrap gap-2"><div className="relative"><Search size={16} className="absolute left-3 top-3 muted"/><input className="input pl-9" placeholder="Search players" value={search} onChange={e=>setSearch(e.target.value)}/></div><select className="input" value={classFilter} onChange={e=>setClassFilter(e.target.value)}><option value="all">All classes</option>{[...new Set(players.map(p=>p.class_year).filter(Boolean))].sort().map(y=><option key={y} value={y}>{y}</option>)}</select><select className="input" value={statusFilter} onChange={e=>setStatusFilter(e.target.value)}><option value="all">All statuses</option><option value="active">Active</option><option value="pending">Pending</option><option value="declined">Declined</option><option value="revoked">Revoked</option></select></div></div><div className="overflow-x-auto mt-5"><table className="w-full text-sm"><thead><tr className="border-b text-left"><th className="py-3 w-8"></th><th className="py-3">Player</th><th className="py-3">Class</th><th className="py-3">Colleges</th><th className="py-3">Coaches</th><th className="py-3">Interactions</th><th className="py-3">Follow-ups</th><th className="py-3">Last activity</th><th></th></tr></thead><tbody>{filtered.map(p=><tr key={p.id} className="border-b last:border-0"><td className="py-3"><input type="checkbox" checked={selected.includes(p.id)} onChange={e=>setSelected(s=>e.target.checked?[...s,p.id]:s.filter(x=>x!==p.id))}/></td><td className="py-3"><div className="font-bold">{p.name}</div><div className="muted text-xs">{p.school_name||p.email}</div><div className="muted text-xs mt-1">{statusLabel(p.status)}{p.relationship_type?` · ${p.relationship_type}`:''}</div></td><td>{p.class_year||'—'}</td><td>{p.collegeCount}</td><td>{p.coachCount}</td><td>{p.interactionCount}</td><td><span className={p.overdue?'text-red-600 font-bold':''}>{p.openFollowups}{p.overdue?` · ${p.overdue} overdue`:''}</span></td><td>{p.lastActivity||'None'}</td><td><Link className="inline-flex items-center gap-1 font-bold" href={`/advisors/player/${p.id}`}>View <ArrowRight size={14}/></Link></td></tr>)}</tbody></table>{loading&&<div className="py-10 text-center muted">Loading players...</div>}{!loading&&!filtered.length&&<div className="py-10 text-center muted">No players match your filters.</div>}</div></section>{compare.length>1&&<section className="card p-5 mt-6"><div className="flex items-center justify-between"><div><h2 className="font-black text-lg">Compare Players</h2><p className="muted text-sm">Compare recruiting activity, not athletic ability.</p></div><BarChart3 size={20}/></div><div className="overflow-x-auto mt-4"><table className="w-full text-sm"><thead><tr className="border-b"><th className="text-left py-3">Metric</th>{compare.map(p=><th key={p.id} className="text-left py-3">{p.name}</th>)}</tr></thead><tbody>{[['Class',p=>p.class_year||'—'],['GPA',p=>p.gpa||'—'],['Colleges',p=>p.collegeCount],['Coaches',p=>p.coachCount],['Interactions',p=>p.interactionCount],['Open follow-ups',p=>p.openFollowups],['Overdue',p=>p.overdue],['Last activity',p=>p.lastActivity||'None']].map(([label,fn]:any)=><tr key={label as string} className="border-b"><td className="py-3 font-bold">{label}</td>{compare.map(p=><td key={p.id} className="py-3">{fn(p)}</td>)}</tr>)}</tbody></table></div></section>}<section className="card p-5 mt-6"><div className="flex items-center justify-between"><div><h2 className="font-black text-lg">Advisor Invitations</h2><p className="muted text-sm">Pending relationships do not grant access.</p></div><Clock3 size={19}/></div><div className="mt-4 space-y-2">{assignments.filter(a=>a.status==='pending').map(a=><div key={a.id} className="border rounded-xl p-4 flex items-center gap-3"><div className="flex-1"><div className="font-bold">{players.find(p=>p.id===a.athlete_user_id)?.name||'Player'}</div><div className="muted text-sm">{advisors.find(x=>x.user_id===a.advisor_user_id)?.profile?.full_name||'Advisor'} · {a.relationship_type}</div></div><button className="text-sm font-bold" onClick={()=>cancelInvite(a.id)}>Cancel</button></div>)}{!assignments.some(a=>a.status==='pending')&&<div className="muted py-6 text-center">No pending invitations.</div>}</div></section></div></AppShell>}
