'use client';
import Link from 'next/link';
import {useEffect,useMemo,useState} from 'react';
import {AlertTriangle,ArrowRight,CheckCircle2,Search} from 'lucide-react';
import AppShell from '@/components/AppShell';
import PageHeader from '@/components/PageHeader';
import {createClient} from '@/lib/supabase-browser';
const exact=(i:any)=>i.date_precision==='exact'&&i.date?String(i.date):'';
const days=(d:string)=>d?Math.max(0,Math.floor((Date.now()-new Date(`${d}T12:00:00`).getTime())/86400000)):999;
const today=()=>new Date().toISOString().slice(0,10);
export default function RecruitingBoard(){
 const c=createClient();
 const[rows,setRows]=useState<any[]>([]),[q,setQ]=useState(''),[filter,setFilter]=useState('all'),[loading,setLoading]=useState(true);
 useEffect(()=>{(async()=>{const{data:{user}}=await c.auth.getUser();if(!user)return;const{data:m}=await c.from('organization_members').select('organization_id,role,organization_view_access').eq('user_id',user.id).eq('status','active').maybeSingle();if(!m||!(m.role==='owner'||m.role==='admin'||m.organization_view_access)){setLoading(false);return}const{data:ms}=await c.from('organization_members').select('user_id').eq('organization_id',m.organization_id).eq('role','athlete').eq('status','active');const ids=[...new Set((ms||[]).map((x:any)=>x.user_id))];if(!ids.length){setLoading(false);return}const[ps,schools,coaches,ints,tasks,events,videos]=await Promise.all([c.from('profiles').select('id,full_name,email').in('id',ids),c.from('athlete_colleges').select('athlete_user_id,status,archived_at').in('athlete_user_id',ids),c.from('athlete_coaches').select('athlete_user_id,archived_at').in('athlete_user_id',ids),c.from('interactions').select('athlete_user_id,date,date_precision').in('athlete_user_id',ids).limit(5000),c.from('advisor_tasks').select('athlete_user_id,status,due_date').in('athlete_user_id',ids),c.from('athlete_events').select('athlete_user_id,status,events(date)').in('athlete_user_id',ids),c.from('athlete_videos').select('athlete_user_id').in('athlete_user_id',ids)]);const now=today();setRows((ps.data||[]).map((p:any)=>{const s=(schools.data||[]).filter((x:any)=>x.athlete_user_id===p.id&&!x.archived_at),co=(coaches.data||[]).filter((x:any)=>x.athlete_user_id===p.id&&!x.archived_at),ii=(ints.data||[]).filter((x:any)=>x.athlete_user_id===p.id),dates=ii.map(exact).filter(Boolean).sort().reverse(),last=dates[0]||'',openTasks=(tasks.data||[]).filter((x:any)=>x.athlete_user_id===p.id&&x.status!=='completed'),overdue=openTasks.filter((x:any)=>x.due_date&&x.due_date<now).length,offers=s.filter((x:any)=>x.status==='Offer').length,committed=s.some((x:any)=>x.status==='Committed'),video=(videos.data||[]).some((x:any)=>x.athlete_user_id===p.id),upcoming=(events.data||[]).filter((x:any)=>{if(x.athlete_user_id!==p.id)return false;const ev=Array.isArray(x.events)?x.events[0]:x.events;return Boolean(ev?.date&&ev.date>=now)}).length;const health=Math.round((Math.min(100,s.length*8)+Math.min(100,co.length*10)+(!last?0:Math.max(20,100-days(last)*3))+(video?100:0))/4);let next='Review Player 360°',detail='Review the player’s recruiting picture and decide where staff can help most.',href=`/players/${p.id}`;if(committed){next='Support the commitment',detail='This player is committed. Review any remaining transition or communication tasks.'}else if(overdue){next=`Resolve ${overdue} overdue task${overdue===1?'':'s'}`,detail='Clear overdue work before adding new recruiting tasks.',href=`/messages?tab=tasks&player=${p.id}`}else if(!s.length){next='Help build a school list',detail='No active school relationships are tracked yet.',href=`/advisors/connections?player=${p.id}`}else if(!co.length){next='Add coach contacts',detail='Schools are tracked, but there are no active coach relationships yet.',href=`/advisors/connections?player=${p.id}`}else if(!last){next='Help start recruiting activity',detail='Relationships exist, but no exact-date recruiting activity has been logged yet.',href=`/players/${p.id}`}else if(days(last)>=14){next='Review stale relationships',detail=`No exact-date recruiting activity in ${days(last)} days. Decide which priority relationship should move next.`,href=`/players/${p.id}`}else if(upcoming){next='Prepare for the next event',detail=`${upcoming} upcoming event${upcoming===1?'':'s'} on the calendar. Make sure prep and follow-up are covered.`,href=`/players/${p.id}`};return{id:p.id,name:p.full_name||p.email,schools:s.length,coaches:co.length,activity:ii.length,last,days:days(last),overdue,offers,committed,health,upcoming,next,detail,href}}));setLoading(false)})()},[]);
 const needs=(r:any)=>r.overdue||r.days>=14||r.health<50;
 const visible=useMemo(()=>rows.filter(r=>(!q||r.name.toLowerCase().includes(q.toLowerCase()))&&(filter==='all'||filter==='attention'&&needs(r)||filter==='not_started'&&!r.activity||filter==='no_schools'&&!r.schools||filter==='no_coaches'&&r.schools>0&&!r.coaches||filter==='stale'&&r.days>=14&&r.days<999||filter==='overdue'&&r.overdue>0||filter==='offers'&&r.offers||filter==='committed'&&r.committed)).sort((a,b)=>a.health-b.health),[rows,q,filter]);
 return <AppShell><div className="max-w-7xl mx-auto px-4 sm:px-5 md:px-8 py-6"><PageHeader eyebrow="ORGANIZATION INTELLIGENCE" title="Organization Recruiting Board" subtitle="See every athlete’s recruiting health, relationship coverage and the most useful staff action to take next."/><div className="grid grid-cols-2 lg:grid-cols-4 gap-3 mb-5"><Metric label="Players" value={rows.length}/><Metric label="Need Attention" value={rows.filter(needs).length}/><Metric label="Active Offers" value={rows.reduce((n,r)=>n+r.offers,0)}/><Metric label="Committed" value={rows.filter(r=>r.committed).length}/></div><div className="card p-4 mb-5"><div className="rr-eyebrow">FILTER THE BOARD</div><div className="grid lg:grid-cols-[1fr_260px] gap-3"><label className="flex items-center gap-3 rounded-xl border bg-white px-3 min-h-11"><Search size={18} className="muted shrink-0"/><input className="w-full outline-none bg-transparent" placeholder="Search athlete..." value={q} onChange={e=>setQ(e.target.value)}/></label><select className="input w-full" value={filter} onChange={e=>setFilter(e.target.value)}><option value="all">All Players</option><option value="attention">Needs Attention</option><option value="not_started">No Activity Yet</option><option value="no_schools">No Schools</option><option value="no_coaches">Schools, No Coaches</option><option value="stale">No Activity 14+ Days</option><option value="overdue">Overdue Tasks</option><option value="offers">Has Offers</option><option value="committed">Committed</option></select></div></div>{loading?<div className="card p-10 text-center muted">Building recruiting board...</div>:<div className="grid md:grid-cols-2 xl:grid-cols-3 gap-4">{visible.map(r=><div key={r.id} className="card p-5"><Link href={`/players/${r.id}`} className="block hover:opacity-80"><div className="flex items-start justify-between gap-3"><div><div className="rr-eyebrow">PLAYER 360°</div><h2 className="font-black text-lg">{r.name}</h2><div className="muted text-xs mt-1">{r.schools} schools · {r.coaches} coaches · {r.activity} interactions</div></div><div className="text-right rr-metric-card px-3 py-2 min-w-[76px]"><div className="rr-metric-label">Health</div><div className="rr-metric-value">{r.health}</div></div></div><div className="h-2 bg-slate-100 rounded-full overflow-hidden mt-4"><div className="h-full bg-red-600 rounded-full" style={{width:`${r.health}%`}}/></div><div className="grid grid-cols-3 gap-2 mt-4 text-center"><div className="rr-metric-card p-2"><b>{r.days===999?'—':`${r.days}d`}</b><div className="muted text-[10px]">LAST ACTIVITY</div></div><div className="rr-metric-card p-2"><b>{r.overdue}</b><div className="muted text-[10px]">OVERDUE</div></div><div className="rr-metric-card p-2"><b>{r.committed?'✓':r.offers}</b><div className="muted text-[10px]">{r.committed?'COMMITTED':'OFFERS'}</div></div></div></Link><div className={`mt-4 rounded-xl p-3 ${needs(r)?'rr-priority-card':'bg-slate-50 border'}`}><div className="flex items-start gap-2">{needs(r)?<AlertTriangle size={16} className="mt-0.5 shrink-0"/>:<CheckCircle2 size={16} className="mt-0.5 shrink-0"/>}<div><div className="rr-eyebrow !mb-1">DO THIS NEXT</div><div className="font-black text-sm mt-1">{r.next}</div><div className="muted text-xs mt-1">{r.detail}</div><Link href={r.href} className="inline-flex items-center gap-1 text-sm font-black mt-2">Take action <ArrowRight size={14}/></Link></div></div></div></div>)}</div>}{!loading&&!visible.length&&<div className="card p-10 text-center muted">No players match this view.</div>}</div></AppShell>}
function Metric({label,value}:{label:string;value:number}){return <div className="rr-metric-card p-4"><div className="rr-metric-label">{label}</div><div className="rr-metric-value">{value}</div></div>}
