import AppShell from '@/components/AppShell';import PageHeader from '@/components/PageHeader';import StatCard from '@/components/StatCard';import {createClient} from '@/lib/supabase-server';
export default async function Organization(){const c=await createClient();const {data:{user}}=await c.auth.getUser();const {data:members}=await c.from('organization_members').select('user_id,role,status').eq('user_id',user?.id);const org=members?.[0];return <AppShell><div className="max-w-7xl mx-auto px-5 md:px-8 py-6"><PageHeader title="Organization" subtitle="A shared view across your recruiting program."/><div className="grid grid-cols-2 lg:grid-cols-4 gap-4"><StatCard label="Your role" value={org?.role||'—'}/><StatCard label="Status" value={org?.status||'—'}/><StatCard label="Athletes" value="—" detail="organization-wide"/><StatCard label="Interactions" value="—" detail="this month"/></div><div className="card p-6 mt-6"><h2 className="font-black text-lg">Organization controls</h2><p className="muted mt-2">Admins and owners can manage athletes, advisors, teams, assignments, analytics and exports.</p></div></div></AppShell>}
