import AppShell from '@/components/AppShell';import PageHeader from '@/components/PageHeader';import {createClient} from '@/lib/supabase-server';import {createAdminClient} from '@/lib/supabase-admin';import {AlertTriangle} from 'lucide-react';
export const dynamic='force-dynamic';const label=(v:string)=>({missing_athlete:'Missing athlete',wrong_organization_access:'Wrong organization access',missing_import_history:'Missing import history',google_disconnected:'Google disconnected',wrong_team:'Wrong team',parent_access:'Parent access',account_deletion:'Account deletion',other:'Other'} as any)[v]||v;
export default async function SupportPage(){const c=await createClient();const admin=createAdminClient();const{data:{user}}=await c.auth.getUser();if(!user)return null;const{data:memberships,error:membershipError}=await c.from('organization_members').select('organization_id,role').eq('user_id',user.id).eq('status','active');const me=(memberships||[]).find((m:any)=>m.role==='owner'||m.role==='admin');if(membershipError||!me)return <AppShell><div className="max-w-6xl mx-auto px-5 md:px-8 py-6"><PageHeader title="Support Cases" subtitle="Diagnose organization access and data issues."/><div className="card p-6 border-amber-200 bg-amber-50"><div className="font-black flex gap-2"><AlertTriangle size={18}/>You do not have access to support diagnostics</div><p className="text-sm mt-2">This page is limited to active organization Owners and Admins.</p></div></div></AppShell>;const{data:members,error:memberError}=await admin.from('organization_members').select('user_id').eq('organization_id',me.organization_id).eq('status','active');if(memberError)return <AppShell><div className="max-w-6xl mx-auto px-5 md:px-8 py-6"><PageHeader title="Support Cases" subtitle="Diagnose organization access and data issues."/><div className="card p-6">Support cases are temporarily unavailable because the organization roster could not be loaded.</div></div></AppShell>;const ids=(members||[]).map((m:any)=>m.user_id);const[{data:cases,error:caseError},{data:profiles,error:profileError}]=await Promise.all([admin.from('support_cases').select('id,reporter_user_id,category,status,description,diagnostic_snapshot,created_at').in('reporter_user_id',ids).order('created_at',{ascending:false}).limit(100),admin.from('profiles').select('id,full_name,email').in('id',ids)]);const pm=new Map((profiles||[]).map((p:any)=>[p.id,p]));return <AppShell><div className="max-w-6xl mx-auto px-5 md:px-8 py-6"><PageHeader title="Support Cases" subtitle="Use captured diagnostics to investigate missing athletes, access problems, imports, team membership, and Google connection issues."/>{caseError?<div className="card p-6 border-amber-200 bg-amber-50"><div className="font-black">Support cases are temporarily unavailable</div><p className="text-sm mt-2">The organization is still intact. Try loading this page again later.</p></div>:<div className="space-y-3">{profileError&&<div className="rounded-xl border border-amber-200 bg-amber-50 p-3 text-sm font-semibold">Cases loaded, but some reporter names are temporarily unavailable.</div>}{(cases||[]).map((x:any)=>{const p:any=pm.get(x.reporter_user_id),d=x.diagnostic_snapshot||{};return <div className="card p-5" key={x.id}><div className="flex flex-col sm:flex-row sm:items-start sm:justify-between gap-2"><div><div className="rr-eyebrow">{label(x.category)}</div><div className="font-black text-lg">{p?.full_name||p?.email||'Organization user'}</div><div className="muted text-xs mt-1">Case {String(x.id).slice(0,8).toUpperCase()} · {new Date(x.created_at).toLocaleString()}</div></div><span className="status-pill">{x.status}</span></div><p className="text-sm mt-4">{x.description}</p><div className="grid sm:grid-cols-2 lg:grid-cols-4 gap-2 mt-4 text-xs"><Diag k="Active organizations" v={Array.isArray(d.active_organizations)?d.active_organizations.length:'Not captured'}/><Diag k="Schools" v={d.school_count??'Not captured'}/><Diag k="Coach relationships" v={d.coach_relationship_count??'Not captured'}/><Diag k="Recorded activity" v={d.interaction_count??'Not captured'}/><Diag k="Parent links as parent" v={d.parent_links_as_parent??'Not captured'}/><Diag k="Parent links as athlete" v={d.parent_links_as_athlete??'Not captured'}/><Diag k="Google Calendar" v={d.google_connection?.calendar_connected?'Connected':d.google_connection?'Not connected':'No connection record'}/></div></div>})}{!cases?.length&&<div className="card p-8 text-center"><div className="font-black">No support cases</div><p className="muted text-sm mt-1">Organization users can create a diagnostic support case from Settings when something looks wrong.</p></div>}</div>}</div></AppShell>}
function Diag({k,v}:{k:string;v:any}){return <div className="rounded-xl border p-3"><div className="muted">{k}</div><div className="font-black mt-1">{String(v)}</div></div>}
