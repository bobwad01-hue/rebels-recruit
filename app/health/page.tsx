import Link from 'next/link';import {ArrowRight,Activity,CalendarDays,CheckCircle2,School,Target,Users} from 'lucide-react';import AppShell from '@/components/AppShell';import PageHeader from '@/components/PageHeader';import {createClient} from '@/lib/supabase-server';import {buildRecruitingIntelligence} from '@/lib/recruiting-intelligence';
const one=(v:any)=>Array.isArray(v)?v[0]:v;
const statusLabel=(s:string)=>s==='strong'?'Strong':s==='building'?'Building':'Needs Attention';
const statusIcon=(s:string)=>s==='strong'?<CheckCircle2 size={17}/>:s==='building'?<Activity size={17}/>:<Target size={17}/>;
const scoreTone=(score:number)=>score>=80?'text-emerald-700':score>=65?'text-amber-700':score>=50?'text-orange-700':'text-red-700';
const barTone=(score:number)=>score>=80?'bg-emerald-600':score>=65?'bg-amber-500':score>=50?'bg-orange-500':'bg-red-600';
export default async function RecruitingHealth(){const c=await createClient();const{data:{user}}=await c.auth.getUser();const uid=user?.id;const[schools,coaches,activity,reminders,tasks,athleteEvents]=await Promise.all([c.from('athlete_colleges').select('id,status,college_id,archived_at,colleges(id,name)').eq('athlete_user_id',uid),c.from('athlete_coaches').select('id,coach_id,college_id,last_contact_date,archived_at,colleges(id,name),college_coaches(id,first_name,last_name)').eq('athlete_user_id',uid),c.from('interactions').select('id,type,date,date_precision,created_at').eq('athlete_user_id',uid).order('date',{ascending:false}).limit(500),c.from('reminders').select('id,title,due_date,status').eq('athlete_user_id',uid),c.from('advisor_tasks').select('id,title,due_date,status').eq('athlete_user_id',uid),c.from('athlete_events').select('status,events(id,name,date,type,colleges(id,name))').eq('athlete_user_id',uid)]);const currentSchools=(schools.data||[]).filter((x:any)=>!x.archived_at),currentCoaches=(coaches.data||[]).filter((x:any)=>!x.archived_at),events=(athleteEvents.data||[]).map((x:any)=>({...one(x.events),status:x.status})).filter((x:any)=>x.id);const intelligence=buildRecruitingIntelligence({colleges:currentSchools,coaches:currentCoaches,interactions:activity.data||[],reminders:reminders.data||[],tasks:tasks.data||[],events});return <AppShell><div className="max-w-5xl mx-auto px-4 sm:px-5 md:px-8 py-6"><PageHeader eyebrow="RECRUITING HEALTH" title="How Is My Recruiting Going?" subtitle="Recruiting Health looks at the recruiting work you have recorded and shows where your process is strong and where it may need attention."/><section className="card p-5 sm:p-6"><div className="flex flex-col md:flex-row md:items-start md:justify-between gap-5"><div><div className="rr-eyebrow">YOUR RECRUITING HEALTH</div><div className="flex items-end gap-3"><div className={`text-5xl font-black ${scoreTone(intelligence.score)}`}>{intelligence.score}</div><div className="pb-1"><div className="font-black">{intelligence.health}</div><div className="muted text-xs">out of 100</div></div></div><p className="text-sm mt-4 max-w-2xl"><span className="font-bold">What this means:</span> This is a check on your recruiting process, not a grade and not a prediction that a school will recruit you.</p><p className="muted text-sm mt-2 max-w-2xl">{intelligence.scoreFormula}</p></div><Link href="/game-plan#next-moves" className="btn btn-red">See My Next Moves <ArrowRight size={16}/></Link></div><div className="h-3 bg-slate-100 rounded-full overflow-hidden mt-6" aria-label={`Recruiting Health ${intelligence.score} out of 100`}><div className={`h-full rounded-full ${barTone(intelligence.score)}`} style={{width:`${intelligence.score}%`}}/></div></section><section className="mt-6"><div className="rr-eyebrow">WHAT IS AFFECTING YOUR HEALTH</div><h2 className="rr-section-title">What is helping and what needs work</h2><p className="rr-section-subtitle">Each factor shows what Rebels Recruit can see in your recruiting activity. Tap one to review the details or take action.</p><div className="grid sm:grid-cols-2 gap-4 mt-5">{intelligence.healthFactors.map(f=><Link href={f.href} key={f.id} className="card p-5 rr-interactive-card"><div className="flex items-start justify-between gap-4"><div className="flex items-center gap-2 font-black">{statusIcon(f.status)}{f.label}</div><span className="status-pill">{statusLabel(f.status)}</span></div><p className="muted text-sm mt-3">{f.detail}</p><div className="flex items-center justify-between gap-3 mt-4 pt-4 border-t"><span className="text-xs font-bold">Effect on Health: {f.impact>0?`+${f.impact}`:f.impact}</span><span className="text-sm font-black inline-flex items-center gap-1">See Details <ArrowRight size={14}/></span></div></Link>)}</div></section><section className="card p-5 sm:p-6 mt-6"><div className="rr-eyebrow">IMPORTANT TO KNOW</div><h2 className="font-black text-xl">Recruiting Health does not measure coach interest</h2><p className="muted text-sm mt-2">Rebels Recruit does not assume a coach is interested because you sent an email. It does not move a school to a new Journey stage automatically. It also does not treat imported activity with only a month, year, or unknown date as recent activity. Recruiting Health reflects the recruiting process you have recorded.</p><div className="grid sm:grid-cols-3 gap-3 mt-5"><Link href="/connections" className="rr-metric-card p-4 rr-interactive-card"><School size={18}/><div className="font-black mt-2">{intelligence.activeSchools} active schools</div><div className="muted text-xs mt-1">See Your Schools</div></Link><Link href="/connections" className="rr-metric-card p-4 rr-interactive-card"><Users size={18}/><div className="font-black mt-2">{intelligence.coachRelationships} coach relationships</div><div className="muted text-xs mt-1">See Coach Relationships</div></Link><Link href="/events" className="rr-metric-card p-4 rr-interactive-card"><CalendarDays size={18}/><div className="font-black mt-2">{intelligence.upcomingEvents} upcoming events</div><div className="muted text-xs mt-1">See Upcoming Events</div></Link></div></section></div></AppShell>}
