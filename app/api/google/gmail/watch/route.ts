import {NextResponse} from 'next/server';import {createClient} from '@/lib/supabase-server';import {startGmailWatch} from '@/lib/gmail-inbound';export async function POST(){const c=await createClient();const{data:{user}}=await c.auth.getUser();if(!user)return NextResponse.json({error:'Please sign in again.'},{status:401});try{const watch=await startGmailWatch(user.id);return NextResponse.json({ok:true,expiration:watch.expiration})}catch(e){return NextResponse.json({error:e instanceof Error?e.message:'Could not start Gmail push.'},{status:500})}}