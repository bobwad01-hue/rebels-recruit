import {NextRequest,NextResponse} from 'next/server';
import {createClient} from '@/lib/supabase-server';
import {createAdminClient} from '@/lib/supabase-admin';
const ENTITY='organization_calendar_event_rsvp';const valid=new Set(['going','not_going']);
async function membership(admin:any,userId:string,organizationId?:string|null){let q=admin.from('organization_members').select('organization_id,role,organization_view_access').eq('user_id',userId).eq('status','active');if(organizationId)q=q.eq('organization_id',organizationId);const {data}=await q.limit(1).maybeSingle();return data||null}
async function attendance(admin:any,organizationId:string,eventId:string){const {data:ms}=await admin.from('organization_members').select('user_id,role').eq('organization_id',organizationId).eq('status','active');const athleteIds=(ms||[]).filter((m:any)=>m.role==='athlete').map((m:any)=>m.user_id);if(!athleteIds.length)return {going:[],notGoing:[]};const {data:logs}=await admin.from('audit_log').select('entity_id,actor_user_id,metadata,created_at').eq('entity_type',ENTITY).eq('entity_id',eventId).in('actor_user_id',athleteIds).order('created_at',{ascending:false});const {data:profiles}=await admin.from('profiles').select('id,full_name,email').in('id',athleteIds);const pm=new Map((profiles||[]).map((p:any)=>[p.id,p]));const seen=new Set<string>(),going:any[]=[],notGoing:any[]=[];for(const row of logs||[]){if(seen.has(row.actor_user_id))continue;seen.add(row.actor_user_id);const s=String((row.metadata as any)?.status||'');if(!valid.has(s))continue;const p:any=pm.get(row.actor_user_id)||{};const item={athleteUserId:row.actor_user_id,name:p.full_name||p.email||'Athlete'};(s==='going'?going:notGoing).push(item)}return {going,notGoing}}
export async function GET(req:NextRequest){const c=await createClient();const {data:{user}}=await c.auth.getUser();if(!user)return NextResponse.json({error:'Please sign in again.'},{status:401});const admin=createAdminClient(),scope=req.nextUrl.searchParams.get('scope')||'me',eventId=req.nextUrl.searchParams.get('eventId');if(eventId){const organizationId=eventId.split(':')[1]||'';const member=await membership(admin,user.id,organizationId);if(!member)return NextResponse.json({error:'You do not have access to this event.'},{status:403});return NextResponse.json({eventId,...await attendance(admin,organizationId,eventId)})}if(scope!=='organization'){const {data,error}=await admin.from('audit_log').select('entity_id,metadata,created_at').eq('actor_user_id',user.id).eq('entity_type',ENTITY).order('created_at',{ascending:false});if(error)return NextResponse.json({error:error.message},{status:500});const seen=new Set<string>(),statuses:Record<string,string>={};for(const row of data||[]){const id=String(row.entity_id||'');if(!id||seen.has(id))continue;seen.add(id);const s=String((row.metadata as any)?.status||'');if(valid.has(s))statuses[id]=s}return NextResponse.json({statuses})}const member=await membership(admin,user.id);if(!member)return NextResponse.json({events:{}});const canSeeAll=member.role==='owner'||member.role==='admin'||!!member.organization_view_access;let athleteIds:string[]=[];if(canSeeAll){const {data:ms}=await admin.from('organization_members').select('user_id,role').eq('organization_id',member.organization_id).eq('status','active');athleteIds=(ms||[]).filter((m:any)=>m.role==='athlete').map((m:any)=>m.user_id)}else{const {data:as}=await admin.from('athlete_advisor_assignments').select('athlete_user_id').eq('advisor_user_id',user.id).eq('status','active');athleteIds=(as||[]).map((a:any)=>a.athlete_user_id)}if(!athleteIds.length)return NextResponse.json({events:{}});const {data:logs,error}=await admin.from('audit_log').select('entity_id,actor_user_id,metadata,created_at').eq('entity_type',ENTITY).in('actor_user_id',athleteIds).order('created_at',{ascending:false});if(error)return NextResponse.json({error:error.message},{status:500});const {data:profiles}=await admin.from('profiles').select('id,full_name,email').in('id',athleteIds);const pm=new Map((profiles||[]).map((p:any)=>[p.id,p])),seen=new Set<string>(),events:Record<string,{going:any[];notGoing:any[]}>={};for(const row of logs||[]){const key=`${row.entity_id}:${row.actor_user_id}`;if(seen.has(key))continue;seen.add(key);const s=String((row.metadata as any)?.status||'');if(!valid.has(s))continue;const id=String(row.entity_id);if(!events[id])events[id]={going:[],notGoing:[]};const p:any=pm.get(row.actor_user_id)||{},item={athleteUserId:row.actor_user_id,name:p.full_name||p.email||'Athlete'};(s==='going'?events[id].going:events[id].notGoing).push(item)}return NextResponse.json({events})}
export async function POST(req:NextRequest){const c=await createClient();const {data:{user}}=await c.auth.getUser();if(!user)return NextResponse.json({error:'Please sign in again.'},{status:401});let body:any;try{body=await req.json()}catch{return NextResponse.json({error:'Invalid request.'},{status:400})}const eventId=String(body?.eventId||''),status=String(body?.status||'');if(!eventId.startsWith('google:')||!valid.has(status))return NextResponse.json({error:'Event and Going / Not Going status are required.'},{status:400});const organizationId=eventId.split(':')[1]||'';const admin=createAdminClient(),member=await membership(admin,user.id,organizationId);if(!member||member.role!=='athlete')return NextResponse.json({error:'Only athletes can set attendance for organization events.'},{status:403});const {error}=await admin.from('audit_log').insert({organization_id:organizationId,actor_user_id:user.id,action:'organization_calendar_event_rsvp_set',entity_type:ENTITY,entity_id:eventId,metadata:{status,event_name:body?.eventName||null,event_date:body?.eventDate||null}});if(error)return NextResponse.json({error:error.message},{status:500});return NextResponse.json({ok:true,eventId,status,...await attendance(admin,organizationId,eventId)})}
