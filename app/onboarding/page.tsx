'use client';
import Link from 'next/link';import {useState} from 'react';import {ArrowLeft,ArrowRight,Check,Compass,FileSpreadsheet,Handshake,Target,Upload,Flag} from 'lucide-react';import AppShell from '@/components/AppShell';
const steps=[{eyebrow:'Step 1 of 5',title:'You’re not starting over.',text:'If you already have recruiting work somewhere else, bring it with you. Upload an Excel or CSV recruiting-history file and review what will be added before anything changes.',icon:FileSpreadsheet,action:'/import',actionLabel:'Upload recruiting history',skip:'I don’t have anything to import'},{eyebrow:'Step 2 of 5',title:'Build the school list that matters.',text:'Find Schools helps you explore possibilities. Connections is where the schools and coaches you are actually pursuing live.',icon:Compass,action:'/discover',actionLabel:'Find Schools',skip:'Do this later'},{eyebrow:'Step 3 of 5',title:'Keep every relationship together.',text:'Connections keeps each school, coach, conversation, camp and next step in context. Your Journey preserves the recruiting story over time.',icon:Handshake,action:'/connections',actionLabel:'View Connections',skip:'Continue tour'},{eyebrow:'Step 4 of 5',title:'Know exactly what to do next.',text:'Game Plan combines your reminders, advisor-assigned work, weekly goals and recruiting intelligence into clear Next Moves.',icon:Target,action:'/game-plan',actionLabel:'Open Game Plan',skip:'Continue tour'},{eyebrow:'Step 5 of 5',title:'Your Journey builds as you recruit.',text:'Emails, calls, camps, videos and other meaningful recruiting activity become part of your Journey. Rebels Recruit uses that history to keep recommendations current.',icon:Flag,action:'/journey',actionLabel:'View Journey',skip:'Finish'}];
export default function Onboarding(){const[i,setI]=useState(0);const s=steps[i],Icon=s.icon;const next=()=>{if(i<steps.length-1)setI(i+1);else location.href='/dashboard'};return <AppShell><div className="max-w-3xl mx-auto px-4 sm:px-6 py-8"><div className="flex gap-2 mb-6">{steps.map((_,n)=><div key={n} className={`h-2 flex-1 rounded-full ${n<=i?'bg-red-600':'bg-slate-200'}`}/>)}</div><div className="card p-6 sm:p-8"><div className="h-12 w-12 rounded-2xl bg-red-50 border border-red-100 flex items-center justify-center"><Icon className="text-red-600"/></div><div className="text-xs uppercase tracking-[.16em] font-black text-red-600 mt-6">{s.eyebrow}</div><h1 className="text-3xl sm:text-4xl font-black mt-2">{s.title}</h1><p className="text-lg text-slate-600 mt-4 leading-relaxed">{s.text}</p>{i===0&&<div className="rounded-xl bg-slate-50 border p-4 mt-6"><div className="font-black flex items-center gap-2"><Upload size={17}/>Bring your history. Keep your momentum.</div><div className="muted text-sm mt-1">Schools, coaches, communication, camps and recruiting notes can come with you.</div></div>}<div className="flex flex-col sm:flex-row gap-3 mt-8"><Link href={s.action} className="btn btn-red">{s.actionLabel} <ArrowRight size={16}/></Link><button onClick={next} className="btn">{s.skip}</button>{i>0&&<button onClick={()=>setI(i-1)} className="btn sm:ml-auto"><ArrowLeft size={16}/>Back</button>}</div></div><div className="text-center muted text-xs mt-4">You can come back to Profile, Import, Find Schools and Game Plan anytime.</div></div></AppShell>}
