'use client';
import {useEffect,useState} from 'react';import {useSearchParams} from 'next/navigation';import AppShell from '@/components/AppShell';import {createClient} from '@/lib/supabase-browser';
const types=['Email Sent','Email Received','Text','Phone Call','Video Call','X/Twitter DM','Instagram DM','Coach Followed Me','Coach Liked Post','Coach Commented','Questionnaire','College Camp','Campus Visit','Tournament Interaction','Coach Watched Game','Coach Contacted Me','Thank You Message','Other'];const followDays=[1,3,5,7,10,14,30];
export default function NewActivityForm(){const sp=useSearchParams();const [colleges,setColleges]=useState<any[]>([]);const [coaches,setCoaches]=useState<any[]>([]);const [college,setCollege]=useState(sp.get('college')||'');const [coach,setCoach]=useState(sp.get('coach')||'');const [type,setType]=useState('Email Received');const [initiated,setInitiated]=useState('Coach');const [note,setNote]=useState('');const [followDaysValue,setFollowDaysValue]=useState('');const [date,setDate]=useState(new Date().toISOString().slice(0,10));const [message,setMessage]=useState('');useEffect(()=>{const c=createClient();c.auth.getUser().then(async({data:{user}})=>{const {data}=await c.from('athlete_colleges').select('college_id,colleges(id,name)').eq('athlete_user_id',user?.id);setColleges((data||[]).map((x:any)=>x.colleges));});},[]);useEffect(()=>{if(!college){setCoaches([]);return}createClient().from('college_coaches').select('*').eq('college_id',college).order('last_name').then(({data})=>setCoaches(data||[]));},[college]);async function save(e:React.FormEvent){e.preventDefault();setMessage('');const c=createClient();const {data:{user}}=await c.auth.getUser();const {data:interaction,error}=await c.from('interactions').insert({athlete_user_id:user?.id,actor_user_id:user?.id,college_id:college,coach_id:coach||null,type,initiated_by:initiated,date,note}).select('id').single();if(error||!interaction){setMessage(error?.message||'Unable to save interaction.');return}if(followDaysValue){const due=new Date(date);due.setDate(due.getDate()+Number(followDaysValue));const {error:reminderError}=await c.from('reminders').insert({owner_user_id:user?.id,athlete_user_id:user?.id,college_id:college,coach_id:coach||null,interaction_id:interaction.id,title:`Follow up after ${type.toLowerCase()}`,due_date:due.toISOString().slice(0,10)});if(reminderError){setMessage(reminderError.message);return}}location.href='/activity'}return <AppShell><div className="max-w-2xl mx-auto px-5 md:px-8 py-8"><div className="card p-6"><h1 className="text-2xl font-black">Quick Log Interaction</h1><p className="muted mt-1">Capture what happened in about 10–15 seconds.</p><form onSubmit={save} className="space-y-5 mt-7"><label className="block"><span className="text-sm font-bold">College</span><select className="input mt-1" value={college} onChange={e=>{setCollege(e.target.value);setCoach('')}} required><option value="">Choose a college</option>{colleges.map(c=><option value={c.id} key={c.id}>{c.name}</option>)}</select></label><label className="block"><span className="text-sm font-bold">Coach</span><select className="input mt-1" value={coach} onChange={e=>setCoach(e.target.value)} disabled={!college}><option value="">Select a coach</option>{coaches.map(c=><option value={c.id} key={c.id}>{c.first_name} {c.last_name}{c.title?` · ${c.title}`:''}</option>)}</select><p className="muted text-xs mt-1">Add coaches from the college page if the list is empty.</p></label><label className="block"><span className="text-sm font-bold">What happened?</span><select className="input mt-1" value={type} onChange={e=>setType(e.target.value)}>{types.map(t=><option key={t}>{t}</option>)}</select></label><label className="block"><span className="text-sm font-bold">Who initiated it?</span><select className="input mt-1" value={initiated} onChange={e=>setInitiated(e.target.value)}><option>Athlete</option><option>Coach</option><option>Advisor</option><option>Other</option></select></label><label className="block"><span className="text-sm font-bold">Date</span><input className="input mt-1" type="date" value={date} onChange={e=>setDate(e.target.value)} required/></label><label className="block"><span className="text-sm font-bold">Short note</span><textarea className="input mt-1 min-h-24" placeholder="What matters about this interaction?" value={note} onChange={e=>setNote(e.target.value)}/></label><label className="block"><span className="text-sm font-bold">Follow-up reminder</span><select className="input mt-1" value={followDaysValue} onChange={e=>setFollowDaysValue(e.target.value)}><option value="">No follow-up</option>{followDays.map(d=><option value={d} key={d}>{d}-day follow-up</option>)}</select></label>{message&&<div className="text-sm text-red-600">{message}</div>}<button className="btn btn-red w-full">Save interaction</button></form></div></div></AppShell>}
