'use client';
import {useEffect,useMemo,useState} from 'react';
import AppShell from '@/components/AppShell';
import {createClient} from '@/lib/supabase-browser';
function money(v:any){return v==null?'—':new Intl.NumberFormat('en-US',{style:'currency',currency:'USD',maximumFractionDigits:0}).format(v)}
export default function Compare(){const c=createClient();const [schools,setSchools]=useState<any[]>([]),[selected,setSelected]=useState<string[]>([]);useEffect(()=>{(async()=>{const {data:{user}}=await c.auth.getUser();if(!user)return;const {data}=await c.from('athlete_colleges').select('status,colleges(id,name,city,state,division,conference,college_enrichment(*),college_majors(cip_title))').eq('athlete_user_id',user.id).is('archived_at',null);setSchools((data||[]).map((x:any)=>({...x.colleges,journey:x.status})).filter(Boolean))})()},[]);const rows=useMemo(()=>schools.filter(s=>selected.includes(s.id)),[schools,selected]);function toggle(id:string){setSelected(v=>v.includes(id)?v.filter(x=>x!==id):v.length<4?[...v,id]:v)}return <AppShell><div className="max-w-7xl mx-auto px-5 md:px-8 py-8"><h1 className="text-3xl font-black">Compare Schools</h1><p className="muted mt-1">Choose up to four Connections and compare the facts that matter side by side.</p><div className="flex flex-wrap gap-2 mt-6">{schools.map(s=><button key={s.id} onClick={()=>toggle(s.id)} className={`rounded-full border px-3 py-2 text-sm font-bold ${selected.includes(s.id)?'bg-slate-950 text-white':'bg-white'}`}>{s.name}</button>)}</div>{rows.length?<div className="overflow-x-auto mt-7"><table className="min-w-[850px] w-full border-separate border-spacing-0"><thead><tr><th className="text-left p-3 border-b">Compare</th>{rows.map(s=><th key={s.id} className="text-left p-3 border-b text-lg">{s.name}</th>)}</tr></thead><tbody>{[['Location',(s:any)=>[s.city,s.state].filter(Boolean).join(', ')||'—'],['Division',(s:any)=>s.division||'—'],['Journey',(s:any)=>s.journey||'Researching'],['Enrollment',(s:any)=>s.college_enrichment?.undergraduate_enrollment?.toLocaleString()||s.college_enrichment?.enrollment_total?.toLocaleString()||'—'],['Campus setting',(s:any)=>s.college_enrichment?.campus_setting||'—'],['In-state total cost',(s:any)=>money(s.college_enrichment?.total_cost_in_state)],['Out-of-state total cost',(s:any)=>money(s.college_enrichment?.total_cost_out_of_state)],['Room & board',(s:any)=>money(s.college_enrichment?.room_board)],['Religious affiliation',(s:any)=>s.college_enrichment?.religious_affiliation||'None / not reported'],['Weather',(s:any)=>(s.college_enrichment?.climate_profile||[]).join(', ')||'—'],['Majors available',(s:any)=>(s.college_majors||[]).slice(0,8).map((m:any)=>m.cip_title).join(', ')||'Data pending']].map(([label,get]:any)=><tr key={label}><td className="p-3 border-b font-bold text-sm bg-slate-50">{label}</td>{rows.map(s=><td key={s.id} className="p-3 border-b text-sm align-top">{get(s)}</td>)}</tr>)}</tbody></table></div>:<div className="card p-8 mt-7 text-center muted">Select 2–4 schools to start comparing.</div>}</div></AppShell>}
